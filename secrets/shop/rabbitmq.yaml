apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rabbitmq
  namespace: shop
spec:
  refreshInterval: 1h                       # как часто перечитывать Vault (ротация без коммита)
  secretStoreRef: { kind: ClusterSecretStore, name: vault }
  target:
    name: rabbitmq                            # имя Secret, которое ждёт чарт (existingSecret)
    creationPolicy: Owner
  dataFrom:
    - extract: { key: shop/rabbitmq }             # все ключи из secret/data/shop/rabbitmq -> ключи Secret как есть
