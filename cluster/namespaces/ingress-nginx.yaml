apiVersion: v1
kind: Namespace
metadata:
  name: ingress-nginx
  labels:
    # контроллеру нужна capability NET_BIND_SERVICE (порты 80/443) — это baseline, не restricted
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
