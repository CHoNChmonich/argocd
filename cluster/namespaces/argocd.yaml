# Namespace argocd создаёт bootstrap.sh (до Argo); здесь — чтобы метки Pod Security были в git и selfHeal их держал.
apiVersion: v1
kind: Namespace
metadata:
  name: argocd
  labels:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
