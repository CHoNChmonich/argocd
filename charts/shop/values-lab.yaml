# Переопределения для локального кластера (Docker Desktop kind).
image:
  tag: dev            # в лабе образы пушатся с мутабельным тегом; в проде — тег = версия/sha
  pullPolicy: Always  # мутабельный тег + IfNotPresent = нода не заметит новый образ (ловили это вживую)
